#include "main.h"
#include <stdio.h>
#include <stdlib.h>
/**
 * int_abs(int) - main entry point
 *
 * the function computes the absolute value of an integer
 *
 */
int _abs(int x)
{
	
	x = abs(5);
	
	_putchar(x);

	return (0);
}
