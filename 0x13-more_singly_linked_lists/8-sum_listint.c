#include "lists.h"

/**
 * sum_listint - calculates the sum of alll the data in listint_t.
 * @head: Pointer to the head of listint_t
 *
 * Return: if the list is empty - 0.
 * 	else - sum of all the data.
 *
 */

int sum_listint(listint_t *head)
{
	int s = 0;

	while (head != NULL)
	{
		s += head->n
			head = head->next;

	}

	return (sum);
}
