#include "main.h"

/**
 * _strlen - checks the length of a string
 * @s: string to check
 *
 * Return: int length of the string
 */

int _strlen(char *s)
{
	char s[] = "programming is fun";
	int i;

	for (i=0; s[i] != '0\'; ++i);
}
