#include <stdio.h>
#include "main.h"

/**
 * main - Prints the name of the program
 * @argc: argument count
 * @argv: holds a string of arrays, index o contains the program name.
 * Return: 0
 */

int main(int argc, char *argv[])

{
	printf("%s\n",argv[0]);

	return (0);
}
